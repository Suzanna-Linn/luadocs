
---
import DocsLayout from '/src/layouts/DocsLayout.astro';
import DocsCode from '/src/components/DocsCode.astro';
---

<DocsLayout title="Lua & LSL Docs">
<div class="dashboard summary-function">
  <div class="dashboard-header">
    <span class="lua-section"><span class="tag lua-tag">Lua</span><DocsCode code={`ll.ListRandomize(ListVariable: {T}, Stride: number): {any}`} lang="slua" /></span>
    <span class="lsl-section"><span class="tag lsl-tag">LSL</span><DocsCode code={`list llListRandomize(list ListVariable, integer Stride);`} lang="lsl" /></span>
  </div>

  <div class="dashboard-body">
    <div class="dash-col">
      <p>  <p>Returns a list which is a randomized permutation of <b>src</b>.</p>
</p>
      <table class="param-table" id="parameters">
        <tr>
          <td><span class="lua-section"><span class="tag lua-tag">Lua</span>{`{T}`}</span></td>
          <td><span class="lsl-section"><span class="tag lsl-tag">LSL</span>list</span></td>
          <td>ListVariable / src</td>
          <td>A list you want to randomize.</td>
        </tr>
        <tr>
          <td><span class="lua-section"><span class="tag lua-tag">Lua</span>number</span></td>
          <td><span class="lsl-section"><span class="tag lsl-tag">LSL</span>integer</span></td>
          <td>Stride / stride</td>
          <td>number of entries per stride, if less than 1 it is assumed to be 1</td>
        </tr>
      </table>
    </div>

    <div class="dash-col">
      <div class="dash-section-title">Categories</div>
      <ul class="meta-list">
        <li>list</li>
      </ul>
    </div>

    <div class="dash-col">
      <div class="dash-section-title">Tech Specs</div>
      <div class="tech-item"><span class="tech-label">Energy</span><span class="tech-val">10</span></div>
      <div class="tech-item"><span class="tech-label">LSO ID</span><span class="tech-val">197</span></div>

      <div class="label-stack">
        <span class="attr-label bg-mandatory">Use Return</span>
      </div>
    </div>
  </div>
</div>
  <p>This function supports <a href="https://wiki.secondlife.com/wiki/List#Strided_lists" target="_blank">Strided Lists</a>.</p>
  <h2>Specification</h2>
  <p>When you want to randomize the position of every list element, specify a stride of 1. This is perhaps the setting most used.</p>
  <p>If the stride is not a factor of the list length, the <b>src</b> list is returned. In other words, `llGetListLength(<b>src</b>) % stride` must be 0.</p>
  <p>Conceptually, the algorithm selects `llGetListLength(<b>src</b>) / stride` buckets, and then for each bucket swaps in the contents with another bucket.</p>
  <h2>Examples</h2>
<div class="lua-block"><span class="tag lua-tag">Lua</span><DocsCode code={`local dice = {"2", "4", "1", "6", "3", "5"}

function LLEvents.touch_start(events)
    local num_detected = #events
    local shuffled = ll.ListRandomize(dice, 1)
    ll.OwnerSay(ll.List2CSV(shuffled))
end
`} lang="slua" /></div>
<div class="lsl-block"><span class="tag lsl-tag">LSL</span><DocsCode code={`list dice = ["2", "4", "1", "6", "3", "5"];

default
{
    touch_start(integer num_detected) {
        list shuffled = llListRandomize(dice, 1);
        llOwnerSay(llList2CSV(shuffled));
    }
}`} lang="lsl" /></div>
<div class="lua-block"><span class="tag lua-tag">Lua</span><DocsCode code={`local list01 = {"Cold", "pizza", "in", "the", "early", "morning"}

local list_random = ll.ListRandomize(list01, 2)`} lang="slua" /></div>
<div class="lsl-block"><span class="tag lsl-tag">LSL</span><DocsCode code={`list list01 = ["Cold", "pizza", "in", "the", "early", "morning"];

list list_random = llListRandomize(list01, 2);`} lang="lsl" /></div>
  <p>list_random could be:</p>
  <ul>
    <li>["Cold", "pizza", "in", "the", "early", "morning"]</li>
    <li>["Cold", "pizza", "early", "morning", "in", "the"]</li>
    <li>["in", "the", "Cold", "pizza", "early", "morning"]</li>
    <li>["in", "the", "early", "morning", "Cold", "pizza"]</li>
    <li>["early", "morning", "Cold", "pizza", "in", "the"]</li>
    <li>["early", "morning", "in", "the", "Cold", "pizza"]</li>
  </ul>
  <p>Notice that two adjacent elements from the original list are always kept together, because the stride of 2 was specified.</p>
<div class="lua-block"><span class="tag lua-tag">Lua</span><DocsCode code={`local list_random = ll.ListRandomize(list01, 6)`} lang="slua" /></div>
<div class="lsl-block"><span class="tag lsl-tag">LSL</span><DocsCode code={`list list_random = llListRandomize(list01, 6);`} lang="lsl" /></div>
  <p>list_random in this instance is the original list, exactly in the order it already was, because we told it to keep every set of six elements together, and there are only six elements in the list.</p>
  <h2>Notes</h2>
  <p>Bear in mind that the source list will remain unchanged. Instead, a new list will be produced. So, it's important that you capture this with a variable (unless you are acting directly on the results.)</p>
  <h2>See Also</h2>
  <h3>Functions</h3>
  <table>
    <tbody>
      <tr>
        <td>•</td>
        <td><a href="https://wiki.secondlife.com/wiki/LlListSort" target="_blank">llListSort</a></td>
        <td></td>
        <td></td>
        <td></td>
      </tr>
      <tr>
        <td>•</td>
        <td><a href="https://wiki.secondlife.com/wiki/LlFrand" target="_blank">llFrand</a></td>
        <td></td>
        <td></td>
        <td></td>
      </tr>
    </tbody>
  </table>
  <h2>Deep Notes</h2>
  <p>Source</p>
  <p>lsa_randomize(): 'linden\indra\lscript\lscript_library\lscript_alloc.cpp'</p>
  <table>
    <tbody>
      <tr>
        <th>Signature</th>
      </tr>
      <tr>
        <td><div class="lsl-block"><span class="tag lsl-tag">LSL</span><DocsCode code={`
function list llListRandomize( list src, integer stride );
`} lang="lsl" /></div>

</td>
      </tr>
    </tbody>
  </table>

</DocsLayout>
      