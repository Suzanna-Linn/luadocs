
---
import DocsLayout from '/src/layouts/DocsLayout.astro';
import DocsCode from '/src/components/DocsCode.astro';
---

<DocsLayout title="Lua & LSL Docs">
<div class="dashboard summary-function">
  <div class="dashboard-header">
    <span class="lua-section"><span class="tag lua-tag">Lua</span><DocsCode code={`ll.IsFriend(agent_id: uuid): boolean`} lang="slua" /></span>
    <span class="lsl-section"><span class="tag lsl-tag">LSL</span><DocsCode code={`integer llIsFriend(key agent_id);`} lang="lsl" /></span>
  </div>

  <div class="dashboard-body">
    <div class="dash-col">
      <p>  <p>Returns a <a href="https://wiki.secondlife.com/wiki/Category:LSL_Integer/boolean" target="_blank">boolean</a> (an <a href="https://wiki.secondlife.com/wiki/Integer" target="_blank">integer</a>) that is <a href="https://wiki.secondlife.com/wiki/TRUE" target="_blank">TRUE</a> if <b>agent_id</b> and the owner of the <a href="https://wiki.secondlife.com/wiki/Prim" target="_blank">prim</a> the <a href="https://wiki.secondlife.com/wiki/Script" target="_blank">script</a> is in are friends, otherwise <a href="https://wiki.secondlife.com/wiki/FALSE" target="_blank">FALSE</a>.</p>
</p>
      <table class="param-table" id="parameters">
        <tr>
          <td><span class="lua-section"><span class="tag lua-tag">Lua</span>uuid</span></td>
          <td><span class="lsl-section"><span class="tag lsl-tag">LSL</span>key</span></td>
          <td>agent_id</td>
          <td>Agent ID of another agent in the region.</td>
        </tr>
      </table>
    </div>

    <div class="dash-col">
      <div class="dash-section-title">Categories</div>
      <ul class="meta-list">
        <li>avatar</li>
      </ul>
    </div>

    <div class="dash-col">
      <div class="dash-section-title">Tech Specs</div>
      <div class="tech-item"><span class="tech-label">Energy</span><span class="tech-val">10</span></div>
      <div class="tech-item"><span class="tech-label">LSO ID</span><span class="tech-val">542</span></div>

      <div class="label-stack">
        <span class="attr-label bg-mandatory">Use Return</span>
      </div>
    </div>
  </div>
</div>
  <h2>Specification</h2>
  <ul>
    <li>If the prim is owned by a group this function behaves identically to <a href="https://wiki.secondlife.com/wiki/LlSameGroup" target="_blank">llSameGroup</a></li>
    <li>This function will return <a href="https://wiki.secondlife.com/wiki/FALSE" target="_blank">FALSE</a> under the following edge cases:
    <ul>
      <li>If neither the owner, nor the target agent are in the region.</li>
      <li>If the agent_id does not specify an agent.</li>
    </ul>
  
    </li>
  </ul>
  <h2>Caveats</h2>
  <ul>
    <li>You are not your own friend.</li>
  </ul>
  <h2>Examples</h2>
<div class="lua-block"><span class="tag lua-tag">Lua</span><DocsCode code={`ll = llcompat

local function main()
    ll.SensorRepeat("", NULL_KEY, AGENT, 95, PI, 10)
end

function LLEvents.sensor(events)
    local count = #events
    local index = 0
    index = 0
    while index < count do
        local is_is_not = " is NOT "
        if ll.IsFriend(ll.DetectedKey(index)) ~= 0 then
            is_is_not = " is "
        end
        ll.Say(0, ll.DetectedName(index) .. is_is_not .. "a friend.")
        index += 1
    end
end

main()
`} lang="slua" /></div>
<div class="lsl-block"><span class="tag lsl-tag">LSL</span><DocsCode code={`default
{
    state_entry()
    {
        llSensorRepeat("", NULL_KEY, AGENT, 95, PI, 10);
    }

    sensor(integer count)
    {
        integer index;
        for (index = 0; index < count; ++index)
        {
            string is_is_not = " is NOT ";
            if (llIsFriend(llDetectedKey(index)))
            {
                is_is_not = " is ";
            }
            llSay(0, llDetectedName(index) + is_is_not + "a friend.");
        }
    }
}`} lang="lsl" /></div>
  <h2>See Also</h2>
  <h3>Functions</h3>
  <table>
    <tbody>
      <tr>
        <td>•</td>
        <td><a href="https://wiki.secondlife.com/wiki/LlDetectedGroup" target="_blank">llDetectedGroup</a></td>
        <td></td>
        <td></td>
        <td></td>
      </tr>
      <tr>
        <td>•</td>
        <td><a href="https://wiki.secondlife.com/wiki/LlSameGroup" target="_blank">llSameGroup</a></td>
        <td></td>
        <td></td>
        <td></td>
      </tr>
    </tbody>
  </table>
  <h2>Deep Notes</h2>
  <table>
    <tbody>
      <tr>
        <th>Signature</th>
      </tr>
      <tr>
        <td><div class="lsl-block"><span class="tag lsl-tag">LSL</span><DocsCode code={`
function integer llIsFriend( key agent_id );
`} lang="lsl" /></div>

</td>
      </tr>
    </tbody>
  </table>

</DocsLayout>
      