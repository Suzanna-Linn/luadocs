
---
import DocsLayout from '/src/layouts/DocsLayout.astro';
import DocsCode from '/src/components/DocsCode.astro';
---

<DocsLayout title="Lua & LSL Docs">
<div class="dashboard summary-function">
  <div class="dashboard-header">
    <span class="lua-section"><span class="tag lua-tag">Lua</span><DocsCode code={`ll.GetObjectAnimationNames(): {any}`} lang="slua" /></span>
    <span class="lsl-section"><span class="tag lsl-tag">LSL</span><DocsCode code={`list llGetObjectAnimationNames();`} lang="lsl" /></span>
  </div>

  <div class="dashboard-body">
    <div class="dash-col">
      <p>  <p>Returns a list of names of animations playing in the current object</p>
</p>
    </div>

    <div class="dash-col">
      <div class="dash-section-title">Categories</div>
      <ul class="meta-list">
        <li>object_animation</li>
      </ul>
    </div>

    <div class="dash-col">
      <div class="dash-section-title">Tech Specs</div>
      <div class="tech-item"><span class="tech-label">Energy</span><span class="tech-val">10</span></div>
      <div class="tech-item"><span class="tech-label">LSO ID</span><span class="tech-val">506</span></div>

      <div class="label-stack">
        <span class="attr-label bg-mandatory">Use Return</span>
      </div>
    </div>
  </div>
</div>
  <h2>Examples</h2>
<div class="lua-block"><span class="tag lua-tag">Lua</span><DocsCode code={`ll = llcompat

local is_animation_running

local function stop_all_animations()
    local curr_anims = ll.GetObjectAnimationNames()
    ll.Say(0, "stopping all, curr_anims are " .. tostring(curr_anims))
    local length = #curr_anims
    local index = 0
    while index < length do
        local anim = ll.List2String(curr_anims, index)
        ll.Say(0, "Stopping " .. anim)
        ll.StopObjectAnimation(anim)
        -- This check isn't really needed, just included to demonstrate is_animation_running()
        if is_animation_running(anim) ~= 0 then
            ll.Say(0, "ERROR - failed to stop " .. anim .. "!")
        end
        index += 1
    end
end

function is_animation_running(anim)
    local curr_anims = ll.GetObjectAnimationNames()
    return bit32.bnot(ll.ListFindList(curr_anims, {anim}))
end

local function main()
    ll.Say(0, "Hello, Avatar!")
end

function LLEvents.touch_start(events)
    local total_number = #events
    -- Assumes there is an animation with this name in the current object.
    local anim_name = "MyFancyWalk"
    ll.Say(0, "Touched.")
    ll.StartObjectAnimation(anim_name)
    if is_animation_running(anim_name) ~= 0 then
        ll.Say(0, anim_name .. " is playing, which is good")
    end
    ll.Sleep(5.0)
    stop_all_animations()
    if is_animation_running(anim_name) ~= 0 then
        ll.Say(0, anim_name .. " is playing, which is an error!")
    end
end

main()
`} lang="slua" /></div>
<div class="lsl-block"><span class="tag lsl-tag">LSL</span><DocsCode code={`stop_all_animations()
{
    list curr_anims = llGetObjectAnimationNames();
    llSay(0,"stopping all, curr_anims are " + (string) curr_anims);
    integer length = llGetListLength(curr_anims);
    integer index = 0;
    while (index < length)
    {
        string anim = llList2String(curr_anims, index);
        llSay(0, "Stopping " + anim);
        llStopObjectAnimation(anim);
        // This check isn't really needed, just included to demonstrate is_animation_running()
        if (is_animation_running(anim))
        {
            llSay(0, "ERROR - failed to stop " + anim + "!");
        }
        ++index;
    }
}

integer is_animation_running(string anim)
{
    list curr_anims = llGetObjectAnimationNames();
    return ~llListFindList(curr_anims, (list)anim);
}

default
{
    state_entry()
    {
        llSay(0, "Hello, Avatar!");
    }

    touch_start(integer total_number)
    {
        // Assumes there is an animation with this name in the current object.
        string anim_name = "MyFancyWalk";
        llSay(0, "Touched.");
        llStartObjectAnimation(anim_name);
        if (is_animation_running(anim_name))
        {
            llSay(0,anim_name + " is playing, which is good");
        }
        llSleep(5.0);
        stop_all_animations();
        if (is_animation_running(anim_name))
        {
            llSay(0,anim_name + " is playing, which is an error!");
        }
    }
}`} lang="lsl" /></div>
  <h2>Notes</h2>
  <p>The typical uses for this function are to stop all currently playing animations, or to check whether a particular animation is currently playing.</p>
  <p>This function allows you to see all currently playing animations in this object. Normally the returned values will be the names of animations within the object's inventory. In the unusual case that the animation can't be found, the key for the animation will be returned instead (as a string). In either case, the result can be sent to <a href="https://wiki.secondlife.com/wiki/LlStopObjectAnimation" target="_blank">llStopObjectAnimation</a></p>
  <p>Animated objects work by associating a skeleton with a linkset containing one or more rigged mesh primitives. When animations are played by a script in any of the prims in the linkset, the skeleton will animate and any rigged meshes in the linkset will move accordingly. A script running in any prim of the linkset can start, stop or query animations using the new commands. The typical usage of these functions is to do all object animation scripting in the root prim of the linkset; in this scenario, the animations and scripts would all be part of the inventory of this prim, and so of the object as a whole. However, if scripts and animations are present in multiple prims of a linkset, it is important to understand that animations are started, stopped and tracked independently in each prim.</p>
  <h2>See Also</h2>
  <h3>Functions</h3>
  <table>
    <tbody>
      <tr>
        <td>•</td>
        <td><a href="https://wiki.secondlife.com/wiki/LlStartObjectAnimation" target="_blank">llStartObjectAnimation</a></td>
        <td>–</td>
        <td>Start playing an animation in the current object</td>
        <td></td>
      </tr>
      <tr>
        <td>•</td>
        <td><a href="https://wiki.secondlife.com/wiki/LlStopObjectAnimation" target="_blank">llStopObjectAnimation</a></td>
        <td>–</td>
        <td>Stop playing an animation in the current object</td>
        <td></td>
      </tr>
    </tbody>
  </table>
  <h2>Deep Notes</h2>
  <table>
    <tbody>
      <tr>
        <th>Signature</th>
      </tr>
      <tr>
        <td><div class="lua-block"><span class="tag lua-tag">Lua</span><DocsCode code={`
function_t
ll.GetObjectAnimationNames()
`} lang="slua" /></div>
<div class="lsl-block"><span class="tag lsl-tag">LSL</span><DocsCode code={`
function list llGetObjectAnimationNames();
`} lang="lsl" /></div>

</td>
      </tr>
    </tbody>
  </table>

</DocsLayout>
      