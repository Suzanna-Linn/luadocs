
---
import DocsLayout from '/src/layouts/DocsLayout.astro';
import DocsCode from '/src/components/DocsCode.astro';
---

<DocsLayout title="Lua & LSL Docs">
<div class="dashboard summary-event">
  <div class="dashboard-header">
    <span class="lua-section"><span class="tag lua-tag">Lua</span><DocsCode code={`no_sensor()`} lang="slua" /></span>
    <span class="lsl-section"><span class="tag lsl-tag">LSL</span><DocsCode code={`no_sensor()`} lang="lsl" /></span>
  </div>

  <div class="dashboard-body">
    <div class="dash-col">
      <p>  <p>Result of a call to <a href="https://wiki.secondlife.com/wiki/LlSensor" target="_blank">llSensor</a> or <a href="https://wiki.secondlife.com/wiki/LlSensorRepeat" target="_blank">llSensorRepeat</a>.</p>
</p>
    </div>

    <div class="dash-col">
    </div>

    <div class="dash-col">
      <div class="dash-section-title">Tech Specs</div>

      <div class="label-stack">
      </div>
    </div>
  </div>
</div>
  <h2>Caveats</h2>
  <ul>
    <li>sensor/no_sensor are not always the best solution:
    <ul>
      <li>To determine if something is in/out of range is overkill. For this situation use: <a href="https://wiki.secondlife.com/wiki/LlGetObjectDetails" target="_blank">llGetObjectDetails</a> as you will see in the <a href="#Useful_Snippets" target="_blank">Useful Snippets</a> section.</li>
      <li>To determine if an avatar is in the region, try <a href="https://wiki.secondlife.com/wiki/LlGetAgentSize" target="_blank">llGetAgentSize</a></li>
    </ul>
  
    </li>
  </ul>
  <h2>Examples</h2>
<div class="lua-block"><span class="tag lua-tag">Lua</span><DocsCode code={`ll = llcompat

--List all avatars in range.
function LLEvents.on_rez(i)
    ll.Sensor("", "", AGENT_BY_LEGACY_NAME, 96.0, PI)  -- Detect any avatars within a 96 metre radius sphere
end
function LLEvents.sensor(events)
    local number_detected = #events
    local i = 0
    repeat
        ll.OwnerSay(ll.DetectedName(i) .. " is " .. tostring(ll.VecDist(ll.GetPos(), ll.DetectedPos(i))) .. "m away.")
    until not ((function() i += 1 return i end)() < number_detected)
end
function LLEvents.no_sensor()
    ll.OwnerSay("No avatars in range.")
end
`} lang="slua" /></div>
<div class="lsl-block"><span class="tag lsl-tag">LSL</span><DocsCode code={`//List all avatars in range.
default 
{
     on_rez(integer i) 
     {
          llSensor("", "", AGENT_BY_LEGACY_NAME, 96.0, PI);   // Detect any avatars within a 96 metre radius sphere
     }
     sensor(integer number_detected) 
     {
          integer i = 0;
          do 
          {
               llOwnerSay(llDetectedName(i) + " is " + (string) llVecDist(llGetPos(), llDetectedPos(i) ) + "m away.");
          }
          while(++i < number_detected);
     }
     no_sensor() 
     {
          llOwnerSay("No avatars in range.");
     }
}`} lang="lsl" /></div>
  <h2>Useful Snippets</h2>
<div class="lua-block"><span class="tag lua-tag">Lua</span><DocsCode code={`ll = llcompat

--An alternative solution for find out if a user is not in range
--No sensor is used so the above caveat doesn't apply.

local function InRange(uuid, distance)
    local data = ll.GetObjectDetails(uuid, {OBJECT_POS})
    if #data == 0 then
        return 0
    end
    return ll.VecDist(ll.List2Vector(data, 0), ll.GetPos()) <= distance
end
`} lang="slua" /></div>
<div class="lsl-block"><span class="tag lsl-tag">LSL</span><DocsCode code={`//An alternative solution for find out if a user is not in range
//No sensor is used so the above caveat doesn't apply.

integer InRange(key uuid, float distance)
{
    list data = llGetObjectDetails(uuid, [OBJECT_POS]);
    if(data == [])
        return 0;
    return llVecDist(llList2Vector(data, 0), llGetPos()) <= distance;
}`} lang="lsl" /></div>
  <h2>See Also</h2>
  <h3>Functions</h3>
  <table>
    <tbody>
      <tr>
        <td>•</td>
        <td><a href="https://wiki.secondlife.com/wiki/LlSensor" target="_blank">llSensor</a></td>
        <td></td>
        <td></td>
        <td></td>
      </tr>
      <tr>
        <td>•</td>
        <td><a href="https://wiki.secondlife.com/wiki/LlSensorRepeat" target="_blank">llSensorRepeat</a></td>
        <td></td>
        <td></td>
        <td></td>
      </tr>
    </tbody>
  </table>
  <h2>Deep Notes</h2>
  <p>Issues
All Issues ~ <a href="https://secondlife.canny.io/?search=event+AND+no_sensor" target="_blank">Search Canny for related Issues</a></p>
  <table>
    <tbody>
      <tr>
        <td>[](https://jira.secondlife.com/secure/ShowConstantsHelp.jspa?decorator=popup#IssueTypes)</td>
        <td>[](https://jira.secondlife.com/secure/ShowConstantsHelp.jspa?decorator=popup#StatusTypes)</td>
        <td><a href="http://jira.secondlife.com/browse/SVC-2409" target="_blank">SVC-2409</a></td>
        <td><a href="https://wiki.secondlife.com/wiki/Template:Issues/SVC-2409" target="_blank">A</a></td>
        <td></td>
        <td><a href="https://wiki.secondlife.com/wiki/LlSensorRepeat" target="_blank">llSensorRepeat</a> not triggering no_sensor unless a <a href="https://wiki.secondlife.com/wiki/Sensor" target="_blank">sensor</a> event handler is present</td>
      </tr>
    </tbody>
  </table>
  <table>
    <tbody>
      <tr>
        <th>Signature</th>
      </tr>
      <tr>
        <td><div class="lsl-block"><span class="tag lsl-tag">LSL</span><DocsCode code={`
event void no_sensor(  );
`} lang="lsl" /></div>

</td>
      </tr>
    </tbody>
  </table>

</DocsLayout>
      